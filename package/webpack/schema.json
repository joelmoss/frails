{
  "type": "object",
  "properties": {
    "devServer": {
      "anyOf": [
        {
          "description": "Enabled in development mode, but can be enabled/disabled completely by setting to `true`/`false`",
          "type": "boolean"
        },
        { "type": "object" }
      ]
    },
    "sideLoadViews": {
      "description": "Side load views with Javascript and CSS assets. Enabled by default (`true`).",
      "type": "boolean"
    }
  }
}
